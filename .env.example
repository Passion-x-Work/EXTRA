# EXTRA 판정 프록시 환경변수. 이 파일을 .env 로 복사해서 값 채우기. (.env 는 git에 안 올라감)

# 기본 provider: offline | gpt | claude
AI_PROVIDER=gpt

# GPT (유나 개발·테스트용)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Claude (제출·상대 테스트용)
CLAUDE_API_KEY=
CLAUDE_MODEL=claude-opus-4-8

# 프록시 포트
PORT=8787
